<% remote_form_for :snapshots,
    :url => control_cluster_snapshots_path(@cluster) do |f| %>
        
<%= will_paginate_search_field "cluster_#{@cluster.id}_snapshot_search", nil, nil, :list %>

<%= refresh_image 'Refresh', 'snapshots', nil, :list, nil %>

<span class="vr">&nbsp;</span>

<%= delete_snapshots_submit 'Delete' %>

<span class="vr">&nbsp;</span>

<%= restore_snapshots_submit('Restore', @cluster.zones) %>
<%= hidden_field_tag :command, '', :class => :command %>
<%= hidden_field_tag :command_parameter, '', :class => :command_parameter %>

<span class="vr">&nbsp;</span>

<b>Total: <span id="cluster_<%= @cluster.id %>_snapshots_total"><%= @snapshots.total_entries %></span></b>

<hr>

<%= render :partial => 'cluster/snapshots/list' %>
<% end %>
