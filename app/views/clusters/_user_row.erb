<td><%= user_name_login_email_id(user) %></td>
<td>
	<% if user == current_user or @cluster.provider_account.users.include?(user) %>
	    [ <span class="disabled">remove</span> ]
	<% else %>
		[ <%= remove_cluster_user_link 'remove', @cluster, user %> ]
	<% end %>
</td>
