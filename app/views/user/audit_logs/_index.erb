<%= will_paginate_search_field "user_#{current_user.id}_audit_log_search", nil, nil, :list %>

<%= refresh_image 'Refresh', 'audit_logs', nil, :list, nil %>

<span class="vr">&nbsp;</span>

<b>Total: <span id="user_<%= current_user.id %>_audit_logs_total"><%= @audit_logs.total_entries %></span></b>

<hr>

<%= render :partial => 'user/audit_logs/list' %>